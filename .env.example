# Vercel Blob Storage (required for production persistence)
# Get this from your Vercel project's Storage tab after adding Blob storage
BLOB_READ_WRITE_TOKEN=

# Local development uses file-based storage by default when this is not set
